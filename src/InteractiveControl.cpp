#include <InteractiveControl.hpp>

namespace MidiPatcher {
  // 
  // InteractiveControl::InteractiveControl(PortRegistry * portRegistry, std::string delimiter) : InteractiveControl(portRegistry, std::cin, std::cout, delimiter){
  // }
  //
  // InteractiveControl::InteractiveControl(PortRegistry * portRegistry, std::istream &in, std::ostream &out, std::string delimiter) : AbstractControl(portRegistry), CLI(in,out,delimiter) {
  // }

  //
  // void InteractiveControl::sendCommand(std::vector<std::string> &argv){
  //   handleCommand(argv);
  // }
  //
  // void InteractiveControl::respond(std::vector<std::string> &argv){
  //   receivedResponse(argv);
  // }

}
